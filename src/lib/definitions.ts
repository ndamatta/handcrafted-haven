export type Product = {
  id: string;
  title: string;
  image: string;
  description: string;
  price: number;
  seller: string;
};

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};
